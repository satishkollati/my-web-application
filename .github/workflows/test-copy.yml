name: Create Issue and Set Project Status

on:
  workflow_dispatch:
    inputs:
      issue_title:
        description: "Title of the issue"
        required: true
      issue_body:
        description: "Issue body"
        required: false
        default: ""
      project_title:
        description: "Project V2 title (exact match)"
        required: true
      status_option:
        description: "Status (e.g., Todo, In Progress)"
        required: true

jobs:
  process-issue:
    runs-on: ubuntu-latest

    steps:
      - name: Get Project ID
        id: get_project
        run: |
          query='query { viewer { projectsV2(first: 20) { nodes { id title } } } }'
          result=$(gh api graphql -f query="$query")
          project_id=$(echo "$result" | jq -r --arg title "${{ inputs.project_title }}" '.data.viewer.projectsV2.nodes[] | select(.title == $title) | .id')
          echo "project_id=$project_id" >> $GITHUB_OUTPUT

      - name: Create Issue
        id: create_issue
        run: |
          response=$(gh issue create --title "${{ inputs.issue_title }}" --body "${{ inputs.issue_body }}" --json node_id)
          echo "node_id=$(echo "$response" | jq -r '.node_id')" >> $GITHUB_OUTPUT

      - name: Add Issue to Project
        uses: actions/add-to-project@v1  # Verified GitHub Action
        with:
          project-url: https://github.com/users/${{ github.repository_owner }}/projects/  # Placeholder (not used with content-id)
          content-id: ${{ steps.create_issue.outputs.node_id }}
          github-token: ${{ secrets.GH_PAT }}

      - name: Update Status Field
        uses: alex-page/update-project-v2-item-field@v2
        with:
          project-id: ${{ steps.get_project.outputs.project_id }}
          item-id: ${{ steps.add_to_project.outputs.itemId }}
          field-name: "Status"
          value: "${{ inputs.status_option }}"
          github-token: ${{ secrets.GH_PAT }}
